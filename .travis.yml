language: c

sudo: required

matrix:
  include:
    - os: linux
      env: BUILD_TYPE=astyle
    - os: linux
      env: BUILD_TYPE=cppcheck
    - os: linux
      env: BUILD_TYPE=ad9361_generic
    - os: linux
      env: BUILD_TYPE=ad9361_linux

before_install:
  - ./ci/travis/before_install_linux

script:
  - ./ci/travis/run_build.sh
