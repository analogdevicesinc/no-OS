# Maxim board for capi_selftest (basic variant).
#
# This file's presence enables the (basic, ad-apard32690-sl) combo in no_os_build.py; its
# content is NOT merged into the build. CAPI module selection comes from
# ../../basic.conf, and board identity (TARGET_NUM, BOARD_APARD/EVKIT_V1) comes
# from the CMake preset "ad-apard32690-sl" in board_configs/maxim/CMakePresets.json.
