# Raspberry Pi Pico uses the plain UART driver for the IIO transport; no
# board-specific overrides beyond the platform defaults are required.

# The pico IRQ driver is backed by the RP2040 hardware alarms, so it depends on
# the timer driver; enable the timer API for the pico build.
CONFIG_TIMER=y
