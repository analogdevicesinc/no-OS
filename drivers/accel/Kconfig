menuconfig ACCEL
    bool "Accelerometer Drivers"
    default n
    help
      Enable support for accelerometer drivers.

if ACCEL

config ACCEL_ADXL355
    depends on SPI
    bool "Enable the ADXL355 driver"
    default n

config ACCEL_IIO_ADXL355
    depends on SPI
    select ACCEL_ADXL355
    bool "Enable the ADXL355 IIO driver"
    default n

config ACCEL_ADXL355_CAPI
    depends on SPI && !ACCEL_ADXL355
    bool "Enable the ADXL355 CAPI driver"
    default n
    help
      Enable the ADXL355 CAPI driver which uses the CAPI SPI interface
      instead of the no-OS SPI interface. This provides a more modern
      API for interacting with the ADXL355 accelerometer.

endif